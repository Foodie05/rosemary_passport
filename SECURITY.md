# 安全策略

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `master` 与最新已部署版本 | 支持 |
| 更早版本 | 仅在不破坏数据兼容时尽力回移；建议升级 |

代码通过门禁或处于 `SLA-ready` 状态不等于已获得正式 99.9% SLA。生产承诺必须以实际部署、恢复演练、容量测试和外部可用性计量为准。

## 私密报告漏洞

请在仓库的 Security 页面使用 **Report a vulnerability** 创建私有漏洞报告。不要通过公开 Issue、Pull Request、讨论区或日志粘贴漏洞利用步骤、Token、Cookie、密码、密钥、验证码、数据库内容或个人数据。

报告应尽量包含：

- 受影响的提交、版本、端点或组件。
- 前置条件、最小复现步骤和实际影响。
- 已做的数据最小化或脱敏说明。
- 建议修复或缓解方式（如有）。

请仅使用自有账户和数据进行验证；禁止拒绝服务、批量账号探测、访问他人数据、持久化后门或破坏备份。若验证可能影响生产可用性，请先停止并在私有报告中说明所需条件。

## 处理目标

以下是维护目标，不构成对外合同 SLA：

- 2 个工作日内确认收到报告。
- 5 个工作日内完成初步分级和复现判断。
- 已确认的严重漏洞优先缓解，并在兼容迁移、回滚和数据校验通过后发布修复。
- 修复发布且用户有合理升级时间后，再协调公开披露。

数据库修复必须遵循 expand/backfill/validate；不得为了快速关闭漏洞而删除旧列、重写历史凭据或使旧应用无法只读回滚。
