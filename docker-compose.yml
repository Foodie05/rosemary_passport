services:
  postgres:
    image: postgres:16
    container_name: rosm-passport-postgres
    environment:
      POSTGRES_USER: rosm_passport
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Use --env-file apps/passport_server/.env or set DB_PASSWORD}
      POSTGRES_DB: rosm_passport
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - ./ops/postgres/init:/docker-entrypoint-initdb.d:ro
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rosm_passport -d rosm_passport"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
